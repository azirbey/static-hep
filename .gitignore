# Git tracks the full source tree (styles/, README, build config) as well as the
# compiled CSS, which Cloudflare pulls from the repo.
#
# Do not turn this back into an allowlist: what gets deployed to Cloudflare is
# controlled by .assetsignore, not by this file.

node_modules/

# Pre-refactor CSS snapshot used by `npm run diff`
.baseline/

.DS_Store
**/.DS_Store
**/*.htaccess
